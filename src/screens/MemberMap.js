import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const MemberMapScreen = (props) => {

    return (
        <View>
            <Text>Member map</Text>
        </View>
    );
}

export default MemberMapScreen;

const styles = StyleSheet.create({

});